- 2026-09-02: This project already had a `HANDOVER.md` (uppercase, root) predating this skill's install — adopted its existing section structure (Read this first / Last session / Decisions & why / Known issues / Start here next steps / Standing decisions / Verification) rather than forcing the skill template's exact headings. Also worth checking npm/git tags directly before trusting an old handover's "still pending" claims — the prior file said the 1.9.0 release was deliberately left for next session, but it had actually already fully shipped (npm, tags, GitHub releases) by the time this session started; a stale handover read at face value would have re-done or worried about already-finished work.
- 2026-09-04: `git branch --show-current` is worth checking every time before trusting the prior handover's stated branch — it said "Branch: dev" but the repo was actually sitting on `main` (the prior session's merge-up sequence left it there after PR #279 merged). A handover that states a stale branch, taken at face value, would have led straight to committing new work onto the wrong branch. Also: when a session's new work is large and entirely uncommitted (this session: 7 skills + a full 3D component tree, zero commits), say so as the loudest line in "Current state," not buried in Next steps — it's the single fact most likely to cause damage if missed (e.g. committing straight to `main`).
