# Session Handover

Last updated: 2026-08-16 (end of session, second update)

## Repo state (verified at time of writing)

- **4 branches on GitHub: `main`, `dev`, `test`, `beta`** — work commits to `dev`, promotion is `dev → test → beta → main` via PR, no feature branches.
- **`beta` still requires an approving review the author can't self-provide** — promotions there (and to `main`) go through `gh pr merge --admin`. Confirmed safe/expected, not a workaround to flag each time.
- **This session touched both the root `strata-css` package and `strata-css-docs-site/`** — two real framework bugs were found; one fixed (`shadow-sm`/`shadow-lg`, item 1), one found but deliberately left unfixed (dead opacity classes, item 2). Everything else was docs-site work.
- **Both `1.8.16` and `1.8.17` are fully released**: version bumped twice, `CHANGELOG.md` entries added, both **published to npm** (`npm view strata-css version` → `1.8.17`, `dist-tags.latest` confirmed), git tags `v1.8.16`/`v1.8.17` pushed, GitHub Releases created for both, and the GitHub Packages mirror workflow run after each so `github.com/AftabIbrahimKazi/strata/pkgs/npm/strata-css` matches npm. Nothing left pending on either release.
- **A significant amount of this session was SEO/AI-search discoverability work** — see items 13–16. Not asked for up front; grew out of the user asking "why isn't Strata CSS ranking like it used to."
- **Working tree is clean** as of this update — everything below is committed and pushed to `main`.

## What shipped this session (in order)

1. **Core framework bug found and fixed: `shadow-sm`/`shadow-lg` silently applied the wrong shadow.** While enriching the docs site's Shadows page, `shadow-sm`, `shadow`, and `shadow-lg` all rendered the *same* box-shadow. Root cause in `src/registry/registry.js`: the responsive shadow loop registered class names as `shadow-${bp}` for every breakpoint in `BP_KEYS` (`sm`, `md`, `lg`, `xl`, `xxl`) — and since `sm`/`lg` are also the named-scale's own suffixes, that collided character-for-character with the base `shadow-sm`/`shadow-lg` classes. Registration is last-write-wins (`Map.set`), and the responsive loop runs later in the file, so it silently overwrote both. Fixed by dropping the undocumented bare `shadow-${bp}` case entirely (the documented `shadow-{bp}-{variant}` pattern — `shadow-md-sm`, `shadow-lg-lg` — was unaffected). **Verified via computed `box-shadow` in-browser**, not just reading the generated CSS. Pushed through the full pipeline: `dev` (`c85f901`) → PR #224 → `test` → PR #223 (also carried a pre-existing unrelated `test`→`beta` promotion that was already pending) → `beta` → PR #225 → `main`, confirmed present on `main` via `git show origin/main:src/registry/registry.js`. Version bumped to `1.8.16`, `CHANGELOG.md` entry added. **Not yet `npm publish`ed** — that's on the user.
2. **A second real framework bug found but NOT fixed — flagging for a future session.** `bg-opacity-*`, `text-opacity-*`, and `border-opacity-*` (5+5+4 = 14 classes) each set a CSS custom property (`--st-bg-opacity`, `--st-text-opacity`, `--st-border-opacity`) that is **never read back anywhere** in `src/registry/registry.js` or `src/layers/base.js` — grepped for consumers, found zero. Every one of these classes compiles, applies to the element, and has zero visible effect. This is documented prominently (with a live "click it, watch nothing happen" demo) on the Colors, Typography, and Borders docs pages, but the underlying framework bug itself is still present in `main` — only documented, not fixed. Worth a proper fix (either wire the opacity vars into the actual color functions via `color-mix()`, or remove the dead classes) in a future session.
3. **Guides completion pass** (continuing from a previous session that left Data-Attribute States and Versioning without the "verify against source" treatment the other guides got):
   - **Data-Attribute States** — fully rewritten with live interactive demos (new `AttributeDemo.tsx`) for every `data-st-*` attribute, including two previously undocumented ones found by grepping the registry: `data-st-shake` (modal invalid-shake) and `data-st-side` (offcanvas direction). Added a callout that these attributes are *not* gated by the JIT scanner (they live in the always-emitted `st-base` layer), unlike ordinary utility classes.
   - **Versioning & Contributing** — was 3 sentences; rebuilt with a real interactive version-bump simulator (`VersionBumpDemo.tsx`), full commit-prefix table, branch pipeline diagram, pre-release tag table, and package-independent-versioning explanation.
4. **Site-wide "no inline styles" sweep.** The site's own `styles/main.css` states this rule explicitly; several demos added during the guides pass had violated it (`style={{...}}` in React). Removed all of them, converting to Strata's own arbitrary-value utility classes (`w-[220px]`, `top-0 start-0 end-0`, etc.) or, where genuinely no utility covered it (a docs-only shake-demo animation), a named class added to `styles/main.css` — the sanctioned escape hatch. One exception left alone: `app/opengraph-image.tsx` uses Next's `ImageResponse`/Satori renderer, which only supports inline `style` — no `className` path exists there.
5. **Full utilities documentation pass — 11 dedicated pages, 3 enriched.** Previously, `content/utilities.json` + the generic `[slug]/page.tsx` template covered these thinly (one description line + a short class list). Read `src/registry/registry.js` directly for every group rather than trusting the existing docs, and found substantial undocumented surface area and several real "gotchas" (classes that exist but silently do nothing, or that only work in combination with another class). New dedicated pages: **Spacing, Display, Flexbox, Grid, Sizing, Typography, Colors, Borders, Position, Misc, Arbitrary Values.** Enriched in place (stayed on the generic template, smaller gaps): **Shadows, Overflow, Opacity & Visibility.** Highlights of what was found:
   - Spacing: the `0–5` scale ceiling (`m-6` doesn't exist, silently), `ms-auto`/`me-auto` family, padding has no valid `auto`, `!important` prefix works framework-wide (undocumented anywhere before this).
   - Flexbox: `flex-row-reverse`/`flex-wrap-reverse` undocumented, `flex-wrap-reverse` has no responsive form unlike its siblings, `align-content-*` and the full `order-0..5`/`order-first`/`order-last` scale were completely undocumented.
   - Grid: `row-cols-1..6`/`row-cols-auto` (Bootstrap-style auto-column shorthand) was completely undocumented.
   - Typography: an entire `link-*` utility family (`link-primary`, `link-offset-1/2/3`, `link-underline-{color}`, …) was undocumented.
   - Colors/Borders: `bg-{color}-subtle`/`border-{color}-subtle` (16 classes) undocumented — see item 2 for the opacity bug found in the same area.
   - Arbitrary Values: built a from-scratch reference table of all ~24 arbitrary-capable prefixes, verified pattern-by-pattern which support responsive breakpoints and which support `!important` (three don't: `transition-`, `duration-`, `cursor-`).
6. **`Playground.tsx` gained a `bare` prop** (plus JSON-driven `bare`/`multi` group flags for the generic template) after the user reported "the border UI isn't working." Root cause: `Playground`'s preview box hardcodes `border rounded` as its own chrome, which fights with exactly what the Borders/Shadows pages are trying to demonstrate — `border-0`/`shadow-none` were competing against baked-in styling instead of clearly showing removal. Applied `bare` to Borders (all groups except Width/Color, which genuinely need the baseline border present since `border-1..5`/`border-{color}` only set one sub-property each) and to Shadows (all groups).
7. **Interactive layout-bug audit, at the user's explicit request** ("not all the examples are working correctly"). Wrote a Playwright script that clicked every toggle button on every utility page and measured document-relative position/size before and after, flagging large jumps. Found and fixed two real bugs this way: `position-fixed`/`position-absolute` in the Position page's Scheme demo were live-applying and literally escaping/collapsing the preview box (fixed: restricted the live toggle to `static`/`relative` only, moved `absolute`/`fixed`/`sticky` to reference-only badges with a proper grounded example elsewhere on the page); `translate-middle` applied without its assumed `position-relative` parent context was overlapping surrounding page content (fixed: replaced with a real contained avatar+badge-dot example).
8. **Button pulse + primary/secondary differentiation, at the user's request.** This docs site rebrands `--st-primary` to orange (`styles/variables.css`), and `.btn-outline-secondary` (the site's "secondary" button style) was built from that same `--st-primary`/`--st-primary-hover` pair — landing in the same orange family as `.btn-primary`'s own hardcoded orange gradient, making the two buttons hard to tell apart in light mode. Switched secondary to `--st-secondary`/`--st-secondary-hover` (a distinct muted brown-grey) instead. Also shrank the primary button's always-on pulse ring (30px spread @ 0.7 opacity → 10px @ 0.45).
9. **Companion package docs — all 8 packages, in one architectural change.** The `/packages/[slug]` page was a thin marketing card (npm install line, size/license badges, GitHub/npm links) with no actual usage documentation. Rather than hand-write 8 pages, extended the page to render each package's own `README.md` (new `renderRepoMarkdownFromFirstSection()` in `lib/markdown.ts`, reusing the pattern already established for the Contributing page's `CONTRIBUTING.md` render). Chose `README.md` over `CLAUDE.md` after comparing both — `CLAUDE.md` is inconsistent per-package (Shopmap's is almost entirely internal/contributor notes, no usage docs at all), while every package has a substantial, consumer-facing `README.md`. This gave full documentation (install, quick start, options, methods, events, known limitations) for chart, shopmap, modal, forms, picker, offcanvas, flipbook, and skeleton-loader simultaneously, with zero new content authored — it was already sitting in the repo, just never surfaced on the docs site.
10. **A third real bug, found from a user bug report, not a source audit: the `/docs` page's hero buttons and card grid had the plain-text-link hover underline.** Root cause: `app/(docs)/docs/page.tsx` wrapped its *entire* page — hero, feature cards, and the "Where to go next" grid included — in a single `.prose-links` div. That selector is a blanket `a` descendant match with no exclusion, so it applied to `.btn-primary`/`.btn-outline-secondary` CTAs and `.card` links too, even though the site's own convention (a comment in `styles/main.css`) explicitly says button/card/icon-styled links should be excluded. The card grid's `text-decoration-none` didn't help, since the underline is drawn by a `::after` pseudo-element, not real `text-decoration`. Audited every other `prose-links` usage on the site (6 guides, 4 policy pages, the package README-render page) — this was the only offender. Fixed by narrowing the wrapper to just the actual prose section. Verified via `getComputedStyle(el, '::after').width` before/after (confirms the rule no longer applies) and a hover screenshot.
11. **CodeQL security alerts triaged — 3 dismissed as false positives, 1 actually fixed.** GitHub's Code Scanning flagged 4 open alerts. Investigated all 4 by tracing the actual data flow rather than trusting the automated severity label:
    - 3× "Stored cross-site scripting" (High) in `PackagesCarousel.tsx` and `packages/[slug]/page.tsx`, all the same pattern: `pkg.slug`/`pkg.npmName` interpolated into an `href`. Traced both back to `lib/packages.ts`, where they originate from `fs.readdirSync()` on the repo's own local `packages/` directory — build-time, maintainer-controlled data, not user/request input. Dismissed all 3 via the GitHub API (`dismissed_reason: "false positive"`) with a comment explaining the trace.
    - 1× "Shell command built from environment values" (Medium) in `lib/packages.ts:46` — `execSync(\`git log ... -- packages/${slug}\`)`. Same trust situation as above (low *practical* risk), but a legitimate pattern to harden regardless: swapped to `execFileSync("git", [...])`, passing `slug` as a real argument instead of shell-interpolated text. Verified the build still resolves `lastUpdatedDaysAgo` correctly (not silently broken to `null`) before pushing. Confirmed fixed via CodeQL's own re-scan of `main` (`state: "fixed"` on the alert, matching the post-merge commit SHA) — the GitHub UI just hadn't visually refreshed yet when the user first checked.
12. **`1.8.16` release closed out end-to-end.** Walked the user through the manual npm publish (they hold the token, not me) — first attempt failed with `E403` because their generated token lacked the "bypass 2FA" permission; regenerating as a Granular Access Token with that flag enabled fixed it. Caught a near-miss during this: the user initially ran `npm pack`/`npm publish` from inside `strata-css-docs-site/` (a `"private": true` Next.js app, not meant for npm at all) instead of the repo root — `private: true` would have hard-blocked a real publish attempt there regardless, so no actual risk, but worth having caught before they tried the real command. Once published, created git tag `v1.8.16` (pushed), a GitHub Release with changelog notes, and triggered the "Mirror to GitHub Packages" workflow so `strata-css@1.8.16` is on GitHub Packages too — that workflow's other 7 package jobs (modal, picker, forms, etc.) failed as expected, since they didn't change and their versions already exist there; that's normal for this workflow, not a bug. Also deleted a stale `github-pages` GitHub Environment (created back in May, before the site moved to Vercel in August) via the API — confirmed GitHub Pages itself was already disabled (404 on its API) before removing the leftover environment record.
13. **SEO investigation: traced a real, evidence-backed cause for "why isn't Strata CSS ranking like it used to."** The docs site moved from GitHub Pages to Vercel on 2026-08-13 (`docs/index.html` deleted in that commit), and **the old URL (`https://aftabibrahimkazi.github.io/strata`) is now a genuine 404** — confirmed via a live `WebFetch`, not assumed. No redirect was ever set up, so any prior indexing/backlink equity was orphaned rather than transferred — a textbook cause of a ranking drop after a domain move. Also found and fixed: `package.json`'s `homepage` field, in **all 9** `package.json` files (root + all 8 `packages/*`), still pointed at that dead URL — fixed all 9 to the real current docs URL in the same pass. GitHub repo's own "About" metadata (description/homepage/topics) was already correct — checked via `gh api`, no fix needed there.
14. **Added structured data the docs site had none of beyond a bare `SoftwareApplication` block** — directly targeting the user's follow-up concern that AI-generated search results ("Google's AI") had stopped mentioning them by name. Added: a `Person` entity (`author`/`creator`) for Aftab Ibrahim Kazi with `sameAs` linking GitHub and npm; a `sameAs` array on the software entity itself linking npm/GitHub/Socket (the standard schema.org way of telling search/AI systems "these are the same entity's other profiles" — directly answers the later ask "it needs to rank for AI search as well, both website, my npm, my GitHub, my Socket page, they all must show"); and full `FAQPage` schema for the existing FAQ accordion content, previously plain HTML with zero machine-readable Q&A markup. Hit a real React Server Components constraint doing this: `FAQS` was defined inside `Faq.tsx`, a `"use client"` file, and non-component exports from a client-boundary file aren't usable from a server component (`page.tsx`) — build failed with `FAQS.map is not a function`. Fixed by moving the data to a new plain module, `content/faq.ts`, imported by both the client accordion and the server-rendered JSON-LD. Verified the actual rendered JSON-LD in the built HTML (`.next/server/app/index.html`) rather than just trusting the code compiled.
15. **Socket "Supply Chain Security" alerts investigated and explained, not fixed — because they don't need fixing.** User showed 3 Socket package alerts (Dynamic require, Filesystem access, URL strings) and asked if the recent pushes caused them. Traced every one to its exact source line: dynamic `require()` in `src/index.js`/`bin/strata.js` is Strata loading the user's own `strata.config.js` (inherently dynamic — same as Tailwind/PostCSS/webpack config loading); filesystem access is inherent to being a JIT file scanner; the "URL strings" alert bundles two unrelated things — the standard SVG XML namespace URI (`http://www.w3.org/2000/svg`, static string in generated icon CSS, never fetched) and a CDN URL inside a **template string of example code** `bin/strata.js` prints for `strata init` (never fetched by the package itself; matches the already-reviewed, deliberately-kept tradeoff from the existing `project_socket_score_decision` memory). Proved none of the three are new by diffing `v1.8.15..v1.8.17` — the entire diff is 13 lines, all inside the shadow-fix loop from item 1.
16. **`llms.txt` added** (`public/llms.txt`) — a `robots.txt`-style file written for AI crawlers/LLMs rather than traditional search bots: summarizes what Strata CSS is and links to every guide, all 14 utility reference pages, all 10 component reference pages, and all 8 companion package pages. Also surfaced and fixed a real staleness bug while verifying this deployed: the homepage's "What's new in v…" chip (`components/marketing/Hero.tsx`, driven by `lib/npm.ts`'s live npm-registry fetch) was showing `1.8.16` even after `1.8.17` was published, because that fetch has a 1-hour `revalidate` cache and the last deploy's cache window predated the new publish. Not a bug — pushing the `llms.txt` commit forced a fresh Vercel deploy, which re-fetched and now correctly shows `1.8.17`. Confirmed live via `curl` before and after (decoding Next.js's RSC-streamed HTML payload, which splits text into JSON chunks — a plain string-match `grep` silently truncates it, worth remembering for next time).
17. **Explicitly deferred, not forgotten: custom domain.** Confirmed with the user the docs site is on Vercel's Hobby (free) plan with no custom domain — explained that Hobby *does* support custom domains at no cost, and it's the single biggest lever left for both traditional ranking and AI-entity trust (a shared `*.vercel.app` subdomain carries little inherent authority). User said "forget the domain" for now — deliberate deferral, not an oversight. If picked up later, every hardcoded `strata-css-docs-site.vercel.app` reference across the codebase (`robots.ts`, `sitemap.ts`, `layout.tsx`, the JSON-LD in `page.tsx`, all 9 `package.json` `homepage` fields, GitHub repo settings) needs updating together, or this exact "old URL orphaned, nothing points to the new one" problem repeats.

## Recurring technical gotchas (carried forward + new this session)

- **`npm run build`, never `next build` directly** (`prebuild`/`predev` hooks regenerate `styles/strata.output.css`).
- **Verify CLI/framework behavior by reading the actual source, never assume** — this pattern found a real, shipped, undocumented framework bug this session (item 1 above). Don't trust `CLAUDE.md`/`README.md` prose as ground truth for *current* behavior; grep the registry and check the generated CSS output.
- **JIT scanner only reads literal strings inside `class`/`className` attributes** (`CLASS_ATTR_PATTERN` in `src/scanner/scanner.js`). Classes passed through a component prop like `<Playground classes={[...]}>` are **not** scanned, even though they're real string literals in the file — they need explicit `safelist` entries in `strata.config.js`. This bit every dedicated utilities page this session; the fix each time was adding the exact class list to `strata.config.js`'s `safelist`, verified via `grep` on the generated `strata.output.css`.
- **Don't trust a demo just because the class technically applies correctly.** Two categories of bug this session: (a) a class emits *wrong* CSS due to a registration collision (the shadow bug) — only caught by comparing computed styles, not by checking the class exists; (b) a class emits *correct* CSS but the demo context is wrong (`position-fixed` escaping a shared preview box, `translate-middle` without its assumed parent) — only caught by actually clicking through demos and checking layout, not by reading source. Both required going past "does the CSS rule exist" into "does clicking this actually produce the right/expected visual result."
- **Verification workflow (repeated 20+ times this session)**: build → `npm i -D playwright --no-save` (temporary, always removed after) → `npx next start` on a scratch port → screenshot/interact via a throwaway script → clean up (`rm` the script, `npm rm playwright`, kill the port by PID via `netstat`/`taskkill` on Windows — plain `pkill -f "next start"` sometimes leaves the process alive, confirmed by a stale-server false-negative mid-session; killing by PID is reliable).

## Feedback patterns from this session (apply going forward)

- **When the user says an example "isn't working," don't assume — reproduce it, find the actual root cause, then fix it.** Happened three times this session (Borders UI, Shadows duplicate values, the interactive-examples audit) and each had a genuinely different root cause (baked-in Playground chrome, copy-pasted demo values, a real upstream registry bug) — none would have been found by re-reading the same code more carefully without actually clicking through the live page.
- **A framework bug found while doing docs work is worth fixing at the source, not just documenting around it** — did this for the shadow bug (item 1), but explicitly held off on a second one found in the same area (item 2, dead opacity classes) since fixing it wasn't asked for; flagged it instead. Ask before expanding scope from "document accurately" to "fix the underlying bug," but do fix it once asked, and push it through the full branch pipeline like any other change, not as a special-cased shortcut.
- **Scope pushes narrowly — don't bundle unrelated uncommitted work into a requested push.** When asked to push the shadow fix "to main," confirmed via `AskUserQuestion` whether to include the large pile of unrelated uncommitted docs-site work or keep it separate; user chose separate. Applied the same discipline when committing the docs-site pile at the end of this session — it went through its own commit/PR chain, not folded into the framework-fix commits.
- **When asked to keep documentation in sync ("no information discrepancy"), that includes the meta-docs, not just user-facing pages** — `CHANGELOG.md`, `package.json` version, and this `HANDOVER.md` file were all updated as part of closing out this session, not just the docs site content.
- **When told to remove/dismiss/fix something ("if it's not serving a purpose, remove it"), verify it's actually unused before acting, not just old.** Checked the `github-pages` environment was truly dead (GitHub Pages API returned 404, no workflow references it) before deleting it, rather than deleting on the strength of "it looks stale." Same standard applied to the CodeQL false-positive dismissals — traced the actual data flow (`fs.readdirSync` on a local directory) rather than dismissing on the strength of "probably fine."
- **Default to walking the user through credential-gated steps (npm publish) rather than running them — but once explicitly told "run it, the token is verified," actually run it.** First publish: gave a walkthrough, didn't execute (also caught a near-miss: the user briefly ran `npm publish` from the wrong directory). Second publish (`1.8.17`): user said to run it directly, so `npm run publish:stable` was executed directly — it 403'd on the same 2FA issue as before, which is a real outcome to report accurately, not something to retry blindly or paper over. When the user then confirmed the publish had gone through on their end, verified independently via `npm view strata-css version` before proceeding to tag/release/mirror, rather than trusting the claim alone.
- **Destructive/hard-to-reverse GitHub API calls (deleting an environment, dismissing alerts) get flagged before running, even when the user's intent is clear** — the auto-mode classifier blocked an unconfirmed `DELETE` call on the `github-pages` environment; re-explained what the command does and asked for explicit confirmation before retrying. Correct behavior to preserve going forward, not a bug to work around.
- **Once told to stop doing a task step-by-step and just do it ("i dont know why i have to tell you to update them all the time"), extend that to the whole category going forward, not just the one instance asked about.** After that feedback, the tag/Release/Packages-mirror sequence was done automatically and unprompted for the `1.8.17` release, matching what was already promised.
- **For claims outside the repo (search rankings, AI mentions, security scores), verify with a real tool call before answering — and say plainly when a tool is blocked rather than guessing anyway.** Used `WebFetch` to confirm the dead GitHub Pages URL is a real 404, not assumed. Google's own search-results page blocks automated fetches (confirmed by trying); said so directly rather than fabricating an index count. For Socket's score, found and reused an existing repo pattern (`lib/socket.ts`'s curl-with-browser-User-Agent trick, since Cloudflare fingerprints the HTTP client, not just headers) to get the real number instead of giving up at the first 403.

## Where things stand — start here next session

1. **The dead `bg-opacity-*`/`text-opacity-*`/`border-opacity-*` classes (item 2 above) are a real, still-unfixed framework bug.** Worth asking the user whether to fix it next — either wire the custom properties into the actual color declarations via `color-mix()`, or remove the classes entirely since they've never worked. This is the single biggest known code loose end from this session.
2. **Both `1.8.16` and `1.8.17` are fully shipped** — npm (`latest` dist-tag confirmed at `1.8.17`), GitHub Releases, git tags, and GitHub Packages mirror all done for both. Nothing pending on either release.
3. **SEO/AI-discoverability work is substantially done but has two real remaining levers, both requiring the user's own account access, not code:**
   - **Google Search Console** — not yet verified/set up (checked: no verification meta tag on the live site). This is the single biggest lever left — submitting `sitemap.xml` and requesting indexing directly, rather than waiting for passive discovery.
   - **Bing Webmaster Tools** — same, not set up; also feeds Microsoft Copilot and other AI answer engines, easy to overlook if only thinking "Google."
   - Once the user verifies either, they'll have a verification meta tag/file to hand over — wire it into `app/layout.tsx` (or wherever the specific method needs it) in one pass, same pattern as everything else this session.
4. **Custom domain is a known, deliberately deferred lever, not forgotten** — see item 17. Revisit only if the user brings it up; don't re-propose unprompted (same standing rule as the Socket cssnano/CDN tradeoffs).
5. **Check `git status` at the start of next session** before assuming anything is pending — working tree was clean as of this handover update.
6. **Component reference pages** (`app/(docs)/components/[slug]/page.tsx`) still haven't been audited — only `utilities/[slug]` and `packages/[slug]` got attention this session and the previous one. Unknown whether they have similar depth gaps.
7. Everything from prior sessions' "Where things stand" not mentioned above (Socket static metrics drifting) is unchanged and still applies.
