# Dependencies
node_modules/

# Build output — regenerated by `npm run build`
dist/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Logs
npm-debug.log*
*.log

# Benchmark — generated fixtures and result snapshots
benchmark/fixtures/
benchmark/results/

# Environment
.env
.env.local

# Paid/private packages — not published to GitHub, not part of the OSS repo
paid-packages/
examples/flipbook-3d-test.html

# npm auth lives in the global ~/.npmrc. A repo-local one would hold a
# publish token in plain text, one `git add -A` away from being pushed.
.npmrc
